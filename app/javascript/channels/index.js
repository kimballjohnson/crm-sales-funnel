app.use('/post', postRoutes); if (process.env.NODE_ENV === 'production') {   app.use(express.static(path.join(__vclab, "client/build")));   app.get("*", (_, res) => {     res.sendFile(path.join(__vclab, "client/build", "index.html"));   }); }